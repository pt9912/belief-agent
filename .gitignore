# --- Harness / Werkzeuge ---
# Lokale Claude-Code-Einstellungen (maschinenlokal, nicht teilen)
.claude/settings.local.json
# Temporäre d-check-Reparatur-Patches (--repair > fix.patch)
fix.patch
# Scratch / d-check-Ignore-Wurzel
.tmp/
# Vendored Regelwerk-Baseline ist COMMITTET (.harness/baseline/, MR-007);
# nur ephemere Harness-Nebenprodukte ignorieren (Templates bleiben co-located,
# MR-008 — kein Template-Cache).
.harness/cache/

# --- JVM / Java (Zielplattform laut LH-RB-04) ---
# Build-Ausgaben
target/
build/
out/
bin/
*.class
# Gradle (Wrapper-Dateien gradlew/gradle/wrapper/ werden committet)
.gradle/
# Build-Image-Hash (make build; lokaler Reproduzierbarkeits-Anker, Modul 14).
# Wird bei Replay/Modul 12 gepinnt; bis dahin je Build regeneriert.
harness/image-hash.txt
# Maven
.mvn/timing.properties
# Java-Fehlerprotokolle
hs_err_pid*
replay_pid*

# --- IDE / Editor ---
.idea/
*.iml
.vscode/
.settings/
.project
.classpath

# --- OS / Diverses ---
.DS_Store
Thumbs.db
*.log
